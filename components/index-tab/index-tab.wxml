<view>
  <view class="tabs-header">
    <view class="tabs-header__item {{activeTab === index ? 'tabs-header__item-active' : ''}}" data-item="{{index}}" bind:tap="onTabChange" wx:key="title" wx:for="{{tabs}}">
      {{item.title}}
    </view>
  </view>
</view>