<view>
  <view bind:tap="copy" data-url="{{item.url}}" class="resource-item" wx:for="{{resource}}">
    <text>{{item.label}}</text>
    <t-icon style="display: inline;" name="link-1"></t-icon>
    <text class="resource-item__desc">({{item.desc}})</text>
  </view>
</view>