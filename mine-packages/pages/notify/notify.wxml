<view>
  <view class="notify-list">
    <view wx:for="{{notifyList}}" wx:key="id" class="notify-list__item">
      <t-cell title="{{item.title}}" description="{{item.desc}}" align="top" url="{{item.url}}" image="{{item.leftImage}}" hover bordered="{{false}}" />
    </view>
  </view>
</view>