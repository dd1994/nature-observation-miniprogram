<view class="page-container">
  <t-upload
    mediaType="{{['video','image']}}"
    files="{{fileList}}"
    bind:add="handleAdd"
    bind:remove="handleRemove"
  >
  </t-upload>
  <t-input prefixIcon="edit-2" placeholder="添加笔记(选填)" maxcharacter="{{100}}" bind:change="noteChange" />
</view>