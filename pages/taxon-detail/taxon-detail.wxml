<view>
  <plant-detail wx:if="{{taxonGroup === 'Plantae'}}" taxon="{{taxon}}" taxonTree="{{taxonTree}}" taxonTreeLoading="{{taxonTreeLoading}}" />
  <animal-detail wx:if="{{useAnimalDB}}" taxon="{{taxon}}" taxonTree="{{taxonTree}}" taxonTreeLoading="{{taxonTreeLoading}}" />
</view>