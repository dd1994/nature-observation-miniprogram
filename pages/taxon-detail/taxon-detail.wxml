<view>
  <plant-detail wx:if="{{taxonGroup === 'Plantae'}}" taxon="{{taxonDetail}}" taxonTree="{{taxonTree}}" />
  <animal-detail wx:if="{{useAnimalDB}}" taxon="{{taxonDetail}}" taxonTree="{{taxonTree}}" />
</view>