<view>
  <plant-detail wx:if="{{taxonGroup === 'Plantae'}}" taxon="{{taxon}}" taxonTree="{{taxonTree}}" />
  <animal-detail wx:if="{{useAnimalDB}}" taxon="{{taxon}}" taxonTree="{{taxonTree}}" />
</view>