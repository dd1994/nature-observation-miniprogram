<view class="page-container">
  <!-- <observation-detail-topbar userProfile="{{userProfile}}" observedOn="{{observationDetail.observed_on}}" /> -->
  <t-swiper current="{{currentPhoto}}" autoplay="{{true}}" duration="{{500}}" interval="{{3000}}" loop="{{false}}" list="{{swiperList}}" height="{{'600rpx'}}" navigation="{{ { type: 'dots-bar' } }}" t-class="swiper-theme" bind:click="onSwiperClick" />
  <view class="img-bottom-info">
    <user-name-and-avatar userProfile="{{userProfile}}"></user-name-and-avatar>
  </view>
  <!-- <taxon-cell taxon="{{observationDetail}}"></taxon-cell> -->
  <t-tabs split="{{false}}" defaultValue="{{1}}" t-class="custom-tabs">
    <t-tab-panel label="信息" value="0">
      <view wx:if="{{description}}" class="observation-desc">
        <text>{{description}}</text>
      </view>
      <t-cell bordered="{{false}}" wx:if="{{recommend_address_name}}" hover arrow url="{{mapUrl}}">
        <view slot="title" style="display: flex; align-items: center;">
          <t-icon name="location" style="margin-right: 4px;" /> {{recommend_address_name}}
        </view>
        <view class="observed-on-time" slot="description">
          {{formattedObservedOn}}
        </view>
      </t-cell>

      <view wx:if="{{isOwner}}" class="edit-button-wrapper">
        <t-button bind:tap="edit" icon="edit" theme="primary" block size="medium" variant="outline">编辑记录</t-button>
      </view>
    </t-tab-panel>
    <t-tab-panel label="鉴定" value="1">
      <view class="identifications-pannel">
        <identifications identifications="{{identifications}}" userProfile="{{userProfile}}" bind:agreeID="agreeID" />
        <view class="edit-button-wrapper">
          <t-button icon="filter-2" theme="primary" block size="medium" bind:tap="addIdentification">添加鉴定</t-button>
        </view>
      </view>
    </t-tab-panel>
  </t-tabs>
</view>