<view class="page-conatiner">
  <t-swiper
  current="{{currentPhoto}}"
  autoplay="{{true}}"
  duration="{{500}}"
  interval="{{5000}}"
  list="{{swiperList}}"
  navigation="{{ { type: 'dots-bar' } }}"
/>
</view>